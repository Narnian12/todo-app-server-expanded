generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "file:./dev.db"
}

model Todo {
  id    String  @id @unique
  name  String
  info  String
  editing Boolean
}